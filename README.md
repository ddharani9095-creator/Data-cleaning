1.Handling missing values - I check the entire data with help of power query , there is no missing values in the data set.Here no need data cleaning.
Correcting Inconsistent Data - By removing extra space using the trim and clean option in power query. And also use Find and replace method to correct the data.
Removing duplicates - Using power query I used to removed the duplicates from the product id column. And it's shows the uniques records in the data set.
Spillting and merging data - I used  the split the product id cloumn by the new coloumn as 'Manufacturting date' it contains the date, when its was manufactured and another column named as country code. For the same method i used to split and merge in brand and product column named as'Product brand'.
Number Formating - In the power query I used tramsform the 'Manufaturing data' in to format of DD-MM-YYYY by using the Data type  - date formate.It assume the current year.
Conditional formatting - After cleaning the data and transform and load it into excel. I applied the conditional formating  (Data bars)to price column and the column category where electronics present i appiled the conditions. 
